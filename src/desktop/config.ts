/**
 * @module desktop/config
 *
 * Centralized configuration for the desktop frontend.
 * All API calls use this base URL to communicate with the Codara server.
 */

/** API base URL — must match the server's CODARA_SERVER_PORT (default 23981). */
export const API_BASE = "http://localhost:23981";
